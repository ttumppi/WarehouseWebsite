name: Auto update host machine

on:
  push:
    branches:
      - dev-three
jobs:
  pull-new-version:
    runs-on: self-hosted

    steps:
      - name: pull code
        run: |
          cd ~/WarehouseWebsite
          git checkout dev-three
          git pull WarehouseWebsite dev-three
