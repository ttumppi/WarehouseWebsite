name: Auto update host machine

on:
  push:
    branches:
      - main
jobs:
  pull-new-version:
    runs-on: self-hosted

    steps:
      - name: pull code
        run: |
          git pull WarehouseWebsite main
